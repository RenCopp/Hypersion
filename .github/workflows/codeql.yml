name: CodeQL

# CodeQL static analysis for C++ source. Runs on push/PR to main and on
# a weekly schedule so the alerts stay fresh even when no commits land.

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # Sunday 06:30 UTC
    - cron: '30 6 * * 0'

jobs:
  analyze:
    name: Analyze C++
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install build deps
        run: sudo apt-get update && sudo apt-get install -y g++ make

      # Pinned to v3.30.5 (latest stable on the v3 line). The action's
      # release notes flagged v3-line deprecation by Dec 2026; revisit
      # bumping to v4 once it ships.
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3.30.5
        with:
          languages: c-cpp
          build-mode: manual
          queries: security-extended
          # Skip the vendored Fathom Syzygy probe library (BSD-licensed,
          # upstream at basil00/Fathom). We don't maintain that code and
          # diverging from upstream to silence CodeQL warnings would make
          # future upstream pulls painful.
          config: |
            paths-ignore:
              - 'src/fathom/**'

      - name: Build for CodeQL
        run: make build ARCH=x86-64-avx2

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3.30.5
        with:
          category: "/language:c-cpp"
