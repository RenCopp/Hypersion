# Build artefacts
obj/
pgo/
Hypersion
Hypersion.exe
*.exe
*.o
*.obj
*.dll
*.so
*.dylib
*.gcda
*.gcno

# NNUE network files — distributed separately, see docs/NNUE.md.
*.nnue

# Opening books
*.bin

# Datasets and tournament logs (not part of the engine source).
*.pgn
*.pgn.gz
tuner_positions.txt
2025 Database.pgn

# Secrets — NEVER commit. Token file lives at the project root by default.
Hypersion.txt
hypersion.txt
*.token
*.key

# Lichess bot — kept locally, not part of the public repo.
lichess_bot/
lichess-bot-master/
docs/LICHESS-BOT.md
# Local launcher script for the bot (paths point at the user's install).
start-hypersion-lichess.bat
# Auxiliary engines bundled locally (e.g. Fairy-Stockfish for variants
# experimentation) — not part of the engine itself.
engines/

# Release-build staging area + the binary release zips themselves.
release_staging/
release_staging_*/
# Newer release staging directory (used by testing/make_v31_zips.py).
release/
dist/
Hypersion-*-windows-*.zip
Hypersion-*-linux-*.tar.gz
# Stripped exe variants that landed in release/ during v3.1 packaging.
*.exe.stripped

# Backups created by various tuner / analysis scripts (e.g. apply_tuned_values.py
# saves eval_params.h.bak before mutating eval_params.h).
*.bak

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
*.egg

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Build-system caches
.cache/
.ccache/
*.cmake
CMakeCache.txt
CMakeFiles/

# clangd LSP integration — regenerated by `py testing/gen_compile_commands.py`
compile_commands.json

# Test scripts and artefacts. Publish the small reusable validation harness;
# keep generated match data,
# local paths, large suites, tuning inputs, and experiments private.
testing/*
!testing/test_smoke.py
!testing/test_uci_lifecycle.py
!testing/test_smp_soak.py
!testing/test_uci_fuzz.py
!testing/timeman_test.cpp
!testing/BENCH_SIGNATURE
tmp_*.txt
bot_v*.log
bot_v*.err
lichess_games/

# AI-agent configuration and historical development plans are local working
# material, not part of the engine source or release.
.claude/
CLAUDE.md
AUDIT.md
HYPERSION_MASTER_ROADMAP.md
IMPROVEMENT_PLAN.md
PLAN_V3.2.md
RETUNE_PLAN.md
TOMBSTONE_RETEST_PLAN.md
docs/AUDIT-*.md

# Historical local experiment notes (not part of the public engine source).
DIAGNOSTIC_v3.1_LTC.md
PLAN_V3.2_REAL.md
TOURNAMENT_10g.md
